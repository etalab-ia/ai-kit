{
  "name": "@ai-kit/cli",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "uv run ruff check src/ tests/",
    "format": "uv run ruff format src/ tests/",
    "test": "uv run pytest",
    "build": "uv build"
  }
}
